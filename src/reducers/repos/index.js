import reposActions from './repos.actions';
import reposReducer from './repos.reducer';
import reposSelectors from './repos.selectors';
import reposTypes from './repos.types';

export default {
  reposActions,
  reposReducer,
  reposSelectors,
  reposTypes
};
