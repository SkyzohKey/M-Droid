import sharedStyles from '../../bootstrap/sharedStyles';

const styles = {
  wrapper: {
    width: 50,
    height: 50,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 50
  },
  image: {
    width: 30,
    height: 30,
    margin: 8,
    borderRadius: 30
  }
};

export default styles;
